<?php
/**
 * Landing page fields (ads.html / front page).
 *
 * Hybrid: ACF Pro flexible content is used for the section stack. Each layout
 * also carries `show` + `order` so Rule 4 remains intact.
 *
 * @package Domesca_Homes
 */

defined( 'ABSPATH' ) || exit;

require_once THEME_DIR . '/inc/acf/register.php';
